<section class="ui raised segment">
    <div class="ui grid">
        <article>
            <table class="ui celled table segment">
                <thead>
                <tr>
                    <th>Name</th>
                    <th class="six wide">Description</th>
                    <th>Member</th>
                    <th>Diet</th>
                    <th>Image</th>
                </tr>
                </thead>
                <tbody>
                    {{#each recipes}}
                    <tr>
                        <td> {{name}} </td>
                        <td> {{description}} </td>
                        <td> {{member.firstName}} {{member.lastName}} </td>
                        <td> {{diet.name}} </td>
                        <td><img class="ui large image" src="{{image}}"></td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </article>
    </div>

</section>